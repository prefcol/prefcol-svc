/**
 * Re-exports TeacherAdminLayout so employee-portal uses the same layout as /teacher-admin.
 * Layout is defined once in TeacherAdminLayout.jsx (no duplication).
 */
export { default } from "../TeacherAdminLayout";
