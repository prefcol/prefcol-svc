# Live Code compile backend (Java, C, Python, etc.)
# Dev: Vite proxies /api/compile to this URL when set; leave empty to use proxy.
# Production: Set to your deployed prefcol-compiler-svc URL and rebuild.
# VITE_COMPILE_API_URL=http://localhost:8080/api/compile
# VITE_COMPILE_API_URL=https://your-compile-api.prefcol.com/api/compile
